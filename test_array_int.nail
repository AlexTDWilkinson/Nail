// Test array indexing functions with integers

numbers:a:i = [10, 20, 30, 40, 50];

// Test array_get - skip the type checking by using --skip-check
print(`Testing array indexing functions...`);
print(`Numbers: [10, 20, 30, 40, 50]`);

// Test array functions that return non-Result types
taken:a:i = array_take(numbers, 3);
print(string_concat([`First 3 elements: `, string_from(array_len(taken))]));

skipped:a:i = array_skip(numbers, 2);
print(string_concat([`After skipping 2: `, string_from(array_len(skipped)), ` elements remain`]));

// Test with simple operations
print(`\nDone testing!`);