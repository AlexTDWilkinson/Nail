f divide(a:i, b:i):i!e {
    if {
        b == 0 => { r e(`Cannot divide by zero`); },
        else => { r a / b; }
    }
}

result:i!e = divide(10, 2);
result:i = dangerous(divide(10, 2));
print(string_from(result));