// Test basic enum declarations
// This tests that enums can be declared and transpiled correctly

enum GameState {
    MainMenu,
    Playing,
    Paused,
    GameOver
}

enum Direction {
    North,
    South,
    East,
    West
}

enum ItemType {
    Weapon,
    Armor,
    Consumable,
    QuestItem
}

// More complex enum
enum PlayerAction {
    Move,
    Attack,
    Defend,
    UseItem,
    Talk,
    Rest
}

print(`Enum declarations test completed`);