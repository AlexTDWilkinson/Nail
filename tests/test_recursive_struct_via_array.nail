// Positive test: a struct may contain itself through an array field, because
// the array introduces indirection so the type is not infinitely sized.
struct Tree {
    children:a:Tree,
    label:s
}
root:Tree = Tree { children = [], label = `root` };
print(root.label);
