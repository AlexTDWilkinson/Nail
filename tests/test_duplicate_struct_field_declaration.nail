// Negative test: a struct declaring the same field twice should FAIL type checking
// because the generated Rust struct would have duplicate fields.
struct Broken {
    value_field:i,
    value_field:s
}
print(`done`);
