nail latest
// Fixture for the import checker unit tests. Valid on its own, but a
// program that brings it in with import must be rejected, because this
// constant's initializer runs at startup and reads the environment.
sandboxed_home:s = danger(env_get(`HOME`));
