// Test stdlib functions

x_value:f = 16.0;

sqrt_result:f = math_sqrt(x_value);

abs_result:f = math_abs(-5.7);

power_result:f = math_pow(2.0, 3.0);

greeting:s = `Hello, World!`;

upper:s = string_to_uppercase(greeting);

numbers:a:i = [1, 2, 3, 4, 5];

arr_len:i = array_len(numbers);

final_str:s =string_from(sqrt_result);

print(final_str);