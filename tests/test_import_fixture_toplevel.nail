// Fixture for the import parser unit tests. Valid on its own, but a
// program that brings it in with import must be rejected, because a
// safe-inserted file may only declare things and this one runs a statement.
f harmless_add(a:i, b:i):i {
    r a + b;
}

print(`this line runs at the top level`);
