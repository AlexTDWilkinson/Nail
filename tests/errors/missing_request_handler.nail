// Error test: http_server without the handle_request function it dispatches to
config:HTTP_Config = HTTP_Config {
    static_mounts = [],
    max_body_bytes = 0,
    timeout_seconds = 0,
    state = hashmap_new()
};
http_server(8080, config);
