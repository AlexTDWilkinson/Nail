// Negative test: repeating a field in a struct literal should FAIL type checking
// because the generated Rust struct literal would set the field twice.
struct Point { x_val:i, y_val:i }
spot:Point = Point { x_val = 1, x_val = 2, y_val = 3 };
print(spot.x_val);
