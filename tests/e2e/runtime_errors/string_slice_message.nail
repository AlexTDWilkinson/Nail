// E2E: string_slice runtime error echoes the invalid range
f show_error(error_msg:e):s {
    print(error_msg);
    r `none`;
}
greeting:s = `hello`;
print(safe(string_slice(greeting, 4, 2), show_error));
