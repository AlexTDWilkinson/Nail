nail latest
// E2E: array_min and array_max runtime errors name the function
f show_error(error_msg:e):i {
    print(error_msg);
    r 0;
}
empty_nums:a:i = [];
print(safe(array_min(empty_nums), show_error));
print(safe(array_max(empty_nums), show_error));
