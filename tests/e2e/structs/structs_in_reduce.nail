// E2E: reduce over array of structs summing a field
struct Score { points:i }
scores:a:Score = [Score { points = 5 }, Score { points = 7 }, Score { points = 8 }];
total_points:i = reduce acc entry in scores from 0 { y acc + entry.points; };
print(total_points);
