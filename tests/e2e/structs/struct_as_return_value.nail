nail latest
// E2E: struct as function return value
struct Coord { x_val:i, y_val:i }
f make_coord(x_input:i, y_input:i):Coord { r Coord { x_val = x_input, y_val = y_input }; }
made:Coord = make_coord(3, 8);
print(made.x_val + made.y_val);
