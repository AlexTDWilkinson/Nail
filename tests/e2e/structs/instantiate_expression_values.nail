// E2E: struct instantiation with expression field values
struct Carton { width:i, height:i }
base_size:i = 4;
crate_box:Carton = Carton { width = base_size * 2 + 1, height = base_size - 1 };
print(crate_box.width);
print(crate_box.height);
