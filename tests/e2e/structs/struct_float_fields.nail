nail latest
// E2E: float struct fields and arithmetic
struct Measure { length:f, width:f }
board:Measure = Measure { length = 2.5, width = 4.0 };
print(board.length + board.width);
print(board.length * 2.0);
