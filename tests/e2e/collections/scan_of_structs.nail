// E2E: scan over an array of structs reads a field of each element
struct Order { name:s, amount:i }
orders:a:Order = [Order { name = `first`, amount = 5 }, Order { name = `second`, amount = 15 }];
running:a:i = scan acc order in orders from 0 { y acc + order.amount; };
print(running);
