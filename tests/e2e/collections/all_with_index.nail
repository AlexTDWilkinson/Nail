nail latest
// E2E: all with index parameter, true and false cases
nums:a:i = [1, 2, 3, 4];
ascending:b = all num index in nums { y num == index + 1; };
print(ascending);
same_as_index:b = all num index in nums { y num == index; };
print(same_as_index);
