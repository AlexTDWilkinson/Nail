nail latest
// A reduce accumulator may shadow an outer variable of a different type; the
// outer binding is intact after the reduce.
total:s = `outer`;
numbers:a:i = [1, 2, 3];
total_sum:i = reduce total num in numbers from 0 {
    y total + num;
};
print(total_sum);
print(total);
