// E2E: safe handles string_slice with start greater than end
f bad_slice(error_msg:e):s {
    print(`slice failed`);
    r `none`;
}
greeting:s = `hello`;
print(safe(string_slice(greeting, 4, 2), bad_slice));
