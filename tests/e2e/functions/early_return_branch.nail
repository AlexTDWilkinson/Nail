// E2E: early return from an if branch before later code
f describe_num(num:i):s {
    if {
        num < 0 -> { r `negative`; },
        else -> { print(`checking further`); }
    }
    r `non-negative`;
}
print(describe_num(-5));
print(describe_num(3));
