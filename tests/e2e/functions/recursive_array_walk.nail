nail latest
// E2E: recursion over an array by index
f sum_from(nums:a:i, index:i):i {
    r if {
        index >= array_length(nums) -> { r 0; },
        else -> { r danger(array_get(nums, index)) + sum_from(nums, index + 1); }
    };
}
values:a:i = [3, 1, 4, 1, 5];
print(sum_from(values, 0));
