nail latest
// E2E: recursive function
f fib(num:i):i {
    r if {
        num <= 1 -> { r num; },
        else -> { r fib(num - 1) + fib(num - 2); }
    };
}
sequence:a:i = map index in array_range(0, 10) { y fib(index); };
print(sequence);
