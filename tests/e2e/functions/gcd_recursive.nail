nail latest
// E2E: recursive greatest common divisor
f gcd_calc(first:i, second:i):i {
    r if {
        second == 0 -> { r first; },
        else -> { r gcd_calc(second, first % second); }
    };
}
print(gcd_calc(48, 18));
print(gcd_calc(17, 5));
