nail latest
// A shadow declared inside a loop body dies with the block; the outer
// binding is untouched afterwards.
counter:i = 1;
loop {
    counter:s = `shadowed in loop`;
    print(counter);
    break;
}
print(counter);
