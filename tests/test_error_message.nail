nail latest
// error_message hands a handler the text inside the error value it received.

f describe_failure(err:e):s {
    r error_message(err);
}

message:s = safe(base64_decode(`!!!not base64!!!`), describe_failure);
print(message);
