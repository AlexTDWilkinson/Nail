// Demonstrate stdlib functions

// Test data
x_value:f = 16.0;
greeting:s = `Hello, Nail!`;
v numbers:a:i = [5, 2, 8, 1, 9];

// Demonstrate functions
sqrt_result:f = math_sqrt(x_value);
upper:s = string_to_uppercase(greeting);
sorted:a:i = array_sort(numbers);
joined:s = array_join([`Result: sqrt = `, to_string(sqrt_result), `, upper = `, upper], ``);

print(joined);