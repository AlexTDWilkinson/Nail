// Test if v (variable) declarations are actually mutable

v counter:i = 0;
print(string_from(counter));

// Try to reassign
counter = 5;
print(string_from(counter));

// Try another reassignment
counter = counter + 1;
print(string_from(counter));